# Pulse – BloodConnect Journey

Phase 1 (ZIP 1)

This archive contains the initial project configuration.

Next ZIPs will add:
- app/
- components/
- assets/

After all ZIPs are extracted, run:

npm install
npm run dev
